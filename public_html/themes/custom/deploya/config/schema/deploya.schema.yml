deploya.settings:
  type: theme_settings
  label: "deploya settings"
  mapping:
    scheme:
      type: string
      label: Color scheme
      constraints:
        Choice:
          # @see deploya_form_system_theme_settings_alter()
          - vanilla-light
          - vanilla-dark
          - byte-light
          - byte-dark
