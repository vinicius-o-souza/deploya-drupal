{#
/**
 * @file
 * Preload the commonly used fonts for the headings and the body copy.
 *
 * Available variables:
 * - deploya_path: Returns the path to the deploya theme.
 */
#}

<link rel="preload" href="{{ deploya_path }}/fonts/Outfit/latin.woff2" as="font" type="font/woff2" crossorigin />
